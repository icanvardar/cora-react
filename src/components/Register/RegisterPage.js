import React, { Fragment } from 'react';
import RegisterForm from './RegisterForm';

export default () => {
    return (
        <div className="page">
            <RegisterForm />
        </div>
    )
}